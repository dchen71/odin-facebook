<% provide(:title, "Home") %>

<div class="text-center col-md-12 col-xs-12"  id="title">
	<div class="col-md-offset-3 col-xs-offset-3 col-md-6 col-xs-6">
		<h1>OdinFacebook</h1>
		<% unless signed_in? %>
			<p>A clone of facebook in which users may friend, like content by other users</p>
			<p>Sign up today!</p>
		<% else %>
			<p>Welcome back <%= current_user.name %></p>
		<% end %>
	</div>
</div>